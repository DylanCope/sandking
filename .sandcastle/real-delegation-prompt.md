# TASK

Prove one real delegated coding change in this disposable Git Project.

Create exactly one file named `sandking-real-delegation.txt` at the Project
root with exactly this UTF-8 content, including the trailing newline:

```text
Real delegation through the pinned Sandcastle Harness.
```

Preserve every existing tracked file byte-for-byte. Do not add, remove, or
modify any other tracked or untracked Project content. Do not inspect or write
outside this Project, access the network, use GitHub, launch another agent, or
change Git configuration.

Commit only `sandking-real-delegation.txt` with commit message
`Prove pinned Sandcastle delegation`. Use command-scoped Git author and
committer identity `Sandcastle Real Worker <real-worker@sandking.invalid>` and
disable commit signing for that command. Verify the worktree is clean and the
new commit contains only the expected file.

Once complete, output `<promise>COMPLETE</promise>`.
